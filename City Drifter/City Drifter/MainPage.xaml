<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:City_Drifter"
             Title="City Drifter"
             x:Class="City_Drifter.MainPage">
    <StackLayout Orientation="Vertical" HorizontalOptions="CenterAndExpand"
                 VerticalOptions="Start">
        <StackLayout Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="Start">
            <Button
                HorizontalOptions="Start"  VerticalOptions="CenterAndExpand"
                Text="|||" Clicked="ShowOptionsTab">
            </Button>
            <Label x:Name="isWalking"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"
               Text="Walking">
            </Label>
            <Switch IsToggled="true"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"/>
            <Label x:Name="isDriving"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"
               Text="Driving">
            </Label>
        </StackLayout>
        <Grid Margin="10,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ColWidth}"/>
                <ColumnDefinition Width="0.7*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Column="0" Orientation="Vertical">
                <ListView x:Name="EmployeeView"
            ItemsSource="{Binding Employees}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <StackLayout Grid.Column="1" Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="End">
                <Label x:Name="Map"
               HorizontalOptions="CenterAndExpand"
               VerticalOptions="CenterAndExpand"
               Text="MAP">
                </Label>
            </StackLayout>
        </Grid>
    </StackLayout>
</ContentPage>