<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:City_Drifter"
             Title="City Drifter"
             x:Class="City_Drifter.MainPage">
    <StackLayout Orientation="Vertical" HorizontalOptions="CenterAndExpand"
                 VerticalOptions="Start">
        <StackLayout Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="Start">
            <Button
                HorizontalOptions="Start"  VerticalOptions="CenterAndExpand"
                Text="|||" Clicked="ShowOptionsTab">
            </Button>
            <Label x:Name="isWalking"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"
               Text="Walking">
            </Label>
            <Switch IsToggled="true"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"/>
            <Label x:Name="isDriving"
               HorizontalOptions="End"
               VerticalOptions="CenterAndExpand"
               Text="Driving">
            </Label>
        </StackLayout>
        <AbsoluteLayout>
            <StackLayout Margin="20"
                     AbsoluteLayout.LayoutBounds="0,0,1,1"
                     AbsoluteLayout.LayoutFlags="All">
                <local:CustomMap x:Name="customMap" IsShowingUser="False" />                
            </StackLayout>

            <!-- Overlay -->
            <StackLayout x:Name="overlay"
                     AbsoluteLayout.LayoutBounds="{Binding rect}"
                     AbsoluteLayout.LayoutFlags="All"
                     BackgroundColor="#E5F6FA"
                     IsVisible="True"
                     HorizontalOptions="StartAndExpand">
                <Label Text="Locations:" FontSize="20" FontAttributes="Bold">
                    </Label>
                <ListView x:Name="LocationView"
            ItemsSource="{Binding Locations}" HasUnevenRows="True"
              SeparatorVisibility="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </AbsoluteLayout>
    </StackLayout>
</ContentPage>